// main.js — students will add JavaScript here as features are built

// Render Lucide icons: replaces every <i data-lucide="..."> with an inline SVG.
// Loaded after the Lucide CDN at the end of <body>, so the global is ready.
if (window.lucide) {
    lucide.createIcons();
}
